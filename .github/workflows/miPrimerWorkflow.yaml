name: My first workflow
on:
    - push
    - workflow_dispatch
jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - run: echo "hello World"
    multi-line_bash:
        runs-on: ubuntu-latest
        steps:
            - run: |
               echo "I am"
               echo "a multiline"
               echo "script"
            - name: echo-goodbye
              run: echo "Bye!"